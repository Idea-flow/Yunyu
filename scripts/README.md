# 脚本目录说明

本目录已按职责统一为两类：

1. `db/`：数据库备份、恢复、同步脚本。
2. `release/`：发布、差异 SQL、升级与回滚脚本。

## 兼容说明

为避免历史命令失效，保留了两个兼容软链接：

1. `mysql -> db`
2. `deploy -> release`

建议后续统一使用新路径：

1. `scripts/db/...`
2. `scripts/release/...`
