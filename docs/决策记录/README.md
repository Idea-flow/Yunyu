# ADR 目录说明

ADR 是 Architecture Decision Record，用于记录重要技术决策。

适合写入 ADR 的内容包括：

- 为什么选择 `Nuxt 4` 而不是其他前端方案
- 为什么选择 `Spring Boot 4 + MyBatis-Plus`
- 为什么选择 `@nuxt/content`
- 为什么采用前后台分离认证策略
- 为什么数据库选择 `PostgreSQL` 或 `MySQL 8`

推荐命名格式：

- `0001-选择-nuxt4-作为前端主框架.md`
- `0002-选择-spring-boot4-作为后端主框架.md`
