# 内容访问控制技术实现方案

## 文档目标

本文档用于定义 `Yunyu` 统一“内容访问控制”能力的技术落地方案，覆盖文章观看权限与文章尾部隐藏内容权限两种范围，并为后续扩展会员、专题、商品资源等访问控制场景提供稳定模型。

## 一、核心技术结论

## 1.1 这是“一套能力”，不是两套功能

文章观看权限与文章尾部隐藏内容权限本质上共用以下要素：

- 访问规则
- 授权主体
- 缓存策略
- 前端拦截交互
- 服务端最终裁决

因此正式结论是：

- 统一建设“内容访问控制”模块
- 不分别建设“文章权限系统”和“尾部隐藏内容权限系统”

## 1.2 服务端必须掌握最终显示权

无论是整篇文章还是尾部隐藏内容，受限内容是否返回，都必须由服务端决定。

原因：

- 只要前端已经拿到真实内容，就无法防止用户通过抓包、源码或调试工具直接看到

所以正式方案是：

- 服务端负责判断并决定是否返回真实内容
- 前端只负责交互触发、访客标识、状态展示和刷新

## 1.3 命名统一使用“访问验证码”

本方案不再使用“邀请码”。

技术字段正式统一为：

- `accessCode`
- `ruleType = ACCESS_CODE | WECHAT_ACCESS_CODE`

用户界面文案可展示为：

- `访问验证码`
- `公众号验证码`

规则归属补充说明：

- `ACCESS_CODE` 是文章级独立配置
- `WECHAT_ACCESS_CODE` 是站点级公众号验证码能力

## 二、统一模型设计

## 2.1 资源范围

统一使用 `resourceScope` 描述当前访问控制作用于什么内容：

- `ARTICLE`
- `ARTICLE_TAIL_HIDDEN`

## 2.2 规则类型

本期只保留：

- `LOGIN`
- `ACCESS_CODE`
- `WECHAT_ACCESS_CODE`

后续可扩展：

- `MEMBER_LEVEL`
- `ORDER_ENTITLEMENT`
- `TOPIC_ACCESS_CODE`

## 2.3 规则元信息

为了让规则本身保持通用，而不是被某个资源范围写死，建议为每个规则定义元信息：

```ts
interface ContentAccessRuleMeta {
  ruleType: ContentAccessRule
  supportedScopes: ContentAccessScope[]
  supportsMultiRule: boolean
  cachePolicy: 'NONE' | 'TEMP_GRANT'
  uiMode: 'LOGIN' | 'CODE_INPUT' | 'QRCODE_AND_CODE'
}
```

示例：

```ts
const ACCESS_CODE_RULE_META: ContentAccessRuleMeta = {
  ruleType: 'ACCESS_CODE',
  supportedScopes: ['ARTICLE', 'ARTICLE_TAIL_HIDDEN'],
  supportsMultiRule: true,
  cachePolicy: 'TEMP_GRANT',
  uiMode: 'CODE_INPUT'
}
```

正式建议：

- 底层 `supportedScopes` 支持多 scope
- 当前产品配置层先只在 `ARTICLE` 开放 `ACCESS_CODE`

## 2.4 授权主体

统一使用：

- `USER`
- `VISITOR`

其中：

- `USER` 适用于登录用户
- `VISITOR` 适用于未登录但通过访问验证码校验的访客

## 2.5 规则组合语义

规则从单值升级为数组配置。

本期正式采用：

- `AND`

即：

- 资源上配置的所有规则都必须满足，访问才通过

例如文章规则：

```json
["LOGIN", "ACCESS_CODE"]
```

表示用户必须同时登录并输入正确的文章访问码。

## 三、现有项目约束

结合当前仓库现状，设计时必须考虑：

1. 后台正文编辑仍以 `contentMarkdown` 为主。
2. 前端 `useMarkdownRenderer` 当前负责 Markdown 到 `contentHtml` 的派生。
3. 前台详情页直接消费后端 `contentHtml`。
4. 第一阶段不改 Markdown 语法，不做正文内嵌隐藏块。

这意味着：

- 文章级与尾部隐藏内容级控制都不能只在前端做布尔切换
- 受限内容不能在未授权时直接进入最终返回的 `contentHtml`

## 四、总体架构

统一拆成 5 层：

1. 内容资源建模层
2. 后台配置层
3. 授权判定层
4. 服务端正文与尾部内容组装层
5. 前端访问交互层

图源文件：

- [01-内容访问控制整体结构图.drawio](/Users/wangpenglong/projects/full-stack-project/Yunyu/docs/技术/资源/04-内容访问控制技术实现方案/drawio/01-内容访问控制整体结构图.drawio)
- [01-内容访问控制整体结构图.md](/Users/wangpenglong/projects/full-stack-project/Yunyu/docs/技术/资源/04-内容访问控制技术实现方案/mermaid/01-内容访问控制整体结构图.md)

这张图用于统一说明第一期内容访问控制的分层关系：文章级访问控制与文章尾部隐藏内容共用同一套规则模型、授权缓存和服务端裁决链路。

## 4.1 当前内容权限校验时序图

图源文件：

- [03-当前内容权限校验时序图.drawio](/Users/wangpenglong/projects/full-stack-project/Yunyu/docs/技术/资源/04-内容访问控制技术实现方案/drawio/03-当前内容权限校验时序图.drawio)
- [03-当前内容权限校验时序图.md](/Users/wangpenglong/projects/full-stack-project/Yunyu/docs/技术/资源/04-内容访问控制技术实现方案/mermaid/03-当前内容权限校验时序图.md)

这张图用于说明当前已落地实现的真实访问链路，包括：

- 详情页首屏请求文章详情
- 服务端按文章级和尾部隐藏内容级分别判断是否放行
- 隐藏内容会继承文章级已经通过的同类型规则，避免重复校验
- 前端按优先级只提示当前一条待完成规则
- 校验成功后写入临时授权并刷新详情页

```mermaid
sequenceDiagram
    autonumber
    participant B as 浏览器 / 详情页
    participant A as 站点接口<br/>/api/site/posts/{slug}
    participant S as SiteContentService
    participant G as ContentAccessGrantService
    participant D as post / post_content / content_access_grant

    B->>A: GET /api/site/posts/{slug}
    Note over B,A: 请求文章详情
    A->>A: 解析可选登录态 + 生成访客指纹
    A->>S: getPostDetail(slug, visitorId)
    S->>D: 查询 post / post_content
    S->>G: 计算文章级 pendingRules
    S->>S: 提取文章级已满足规则类型
    S->>G: 计算隐藏内容级 pendingRules\n(继承文章级已满足的同类型规则)
    G->>D: 查询 content_access_grant
    D-->>G: 返回授权结果
    G-->>S: 返回规则命中情况
    S-->>A: 返回 contentHtml / tailHiddenContentHtml / contentAccessState
    A-->>B: articleAccessAllowed / pendingRules / 正文内容

    alt 文章级未通过
        B->>B: 只显示当前最高优先级规则
        alt 当前规则是 LOGIN
            B->>B: 跳转 /login?redirect=当前详情页
            B->>A: 登录后再次 GET /api/site/posts/{slug}
        else 当前规则是 ACCESS_CODE 或 WECHAT_ACCESS_CODE
            B->>A: POST /api/site/posts/{slug}/access/verify
            Note over B,A: 提交 accessCode + scopeType + ruleType
            A->>A: 再次解析可选登录态 + 生成访客指纹
            A->>S: verifyContentAccess(slug, request, visitorId)
            S->>D: 查询 post / post_content
            S->>S: 校验规则是否启用 + 校验验证码
            S->>G: grantAccess(scope, postId, ruleType, userId, visitorIdHash)
            G->>D: 写入或刷新 content_access_grant（7天）
            D-->>G: 写入成功
            G-->>S: 授权完成
            S-->>A: 返回最新 contentAccessState
            A-->>B: granted = true
            B->>A: 再次 GET /api/site/posts/{slug}
        end
    else 文章级已通过
        B->>B: 展示正文内容
        alt 尾部隐藏内容未通过
            B->>B: 仅提示隐藏内容尚未完成的新增规则
            B->>A: 按需 POST /api/site/posts/{slug}/access/verify
            B->>A: 校验成功后再次 GET /api/site/posts/{slug}
        else 尾部隐藏内容也通过
            B->>B: 正文下方展示尾部隐藏内容
        end
        B->>B: 评论区显示在隐藏内容模块下方
    end
```

当前实现补充说明：

- 文章访问与隐藏内容访问仍然是两个独立 scope
- 隐藏内容不是无条件继承文章访问结果，而是只继承“文章已经通过的同类型规则”
- 例如文章和隐藏内容都配置了 `LOGIN + ACCESS_CODE`，当文章已经解锁后，隐藏内容不再重复要求登录和访问码
- 例如文章仅配置了 `LOGIN`，隐藏内容配置了 `LOGIN + WECHAT_ACCESS_CODE`，则隐藏内容只会继续提示 `WECHAT_ACCESS_CODE`

## 五、资源建模方案

## 5.1 文章级访问控制

第一期不建议把文章级访问控制字段全部平铺到表结构中。

更推荐的做法是：

- 在 `post_content` 中增加一个配置 JSON 字段
- 用该 JSON 统一承载文章级与尾部隐藏内容级的访问控制配置

例如：

```json
{
  "version": 1,
  "articleAccess": {
    "enabled": true,
    "ruleTypes": ["LOGIN", "ACCESS_CODE"],
    "articleAccessCode": "ABC123",
    "articleAccessCodeHint": "请输入文章访问码"
  },
  "tailHiddenAccess": {
    "enabled": true,
    "title": "尾部补充内容",
    "ruleTypes": ["WECHAT_ACCESS_CODE"]
  }
}
```

## 5.2 文章尾部隐藏内容访问控制

第一期不做正文内嵌隐藏块，而是改为“文章尾部隐藏内容”。

推荐建模为文章的附加内容区，字段建议：

- `content_access_config_json`
- `tail_hidden_content_markdown`
- `tail_hidden_content_html`

限制说明：

- 尾部隐藏内容的配置元信息放在 `content_access_config_json.tailHiddenAccess`
- 当前产品版本下，尾部隐藏内容配置层不开放 `ACCESS_CODE`
- 尾部隐藏内容本期仍只支持单规则，避免第一期复杂度过高
- 正文内嵌隐藏块作为第二期 `TODO`

## 5.3 统一资源标识

为了让文章与尾部隐藏内容共用一套授权能力，建议统一定义资源标识：

- `resourceScope`
- `resourceId`
- `parentArticleId`

其中：

- 文章级：`resourceScope = ARTICLE`，`resourceId = postId`
- 尾部隐藏内容级：`resourceScope = ARTICLE_TAIL_HIDDEN`，`resourceId = postId + ":tail-hidden"`，`parentArticleId = postId`

## 六、数据模型设计

## 6.1 文章访问控制字段

建议在 `post_content` 中新增：

- `content_access_config_json`

这样服务端在查询文章正文内容时，可以顺带读取访问控制配置。

结构特点：

- 配置型字段统一收敛到一个 JSON 中
- 后续新增规则或扩展属性时不必频繁改表
- 与正文强相关，符合当前项目 `post_content` 的职责边界

## 6.2 尾部隐藏内容存储结构

建议在 `post_content` 中新增并保留：

- `content_access_config_json`
- `tail_hidden_content_markdown`
- `tail_hidden_content_html`

结构特点：

- 尾部隐藏内容的配置元信息仍收敛到 `content_access_config_json`
- 与正文 `content_markdown / content_html` 分离
- 不参与正文 Markdown 解析
- 统一在详情页正文结束后渲染

## 6.3 统一授权表

建议新增统一授权表：

- `content_access_grant`

建议字段：

- `id`
- `resource_scope`
- `resource_id`
- `parent_article_id`
- `rule_type`
- `grant_subject_type`
- `user_id`
- `visitor_id_hash`
- `granted_at`
- `expires_at`
- `status`
- `created_at`
- `updated_at`

作用：

- 支撑文章级授权
- 支撑尾部隐藏内容级授权
- 支撑后续更多范围的访问授权

补充建议：

- 同时增加 `grant_key`

其值可由以下内容计算：

- `resource_scope`
- `resource_id`
- `rule_type`
- `grant_subject_type`
- `user_id / visitor_id_hash`

便于快速判断某一条规则是否已满足。

## 6.4 文章访问码配置

`ACCESS_CODE` 是文章级独立配置，不应放在站点全局配置中，也不建议拆成单独平铺字段。

建议放在：

- `post_content.content_access_config_json.articleAccess`

其中：

- `articleAccess.articleAccessCode` 用于当前文章访问码校验
- `articleAccess.articleAccessCodeHint` 用于前台提示文案或后台辅助说明

## 6.5 公众号验证码配置

`WECHAT_ACCESS_CODE` 是站点级公众号验证码能力，建议统一放在站点配置中：

- `wechat_access_code`
- `wechat_access_code_hint`
- `wechat_qr_code_url`
- `wechat_access_code_expires_in_days`

其中：

- 技术字段中的验证码输入值统一仍叫 `accessCode`
- 前端展示文案优先使用“公众号验证码”

## 七、授权判定服务

## 7.1 统一服务职责

建议新增统一服务，例如：

- `ContentAccessService`

职责：

1. 判断文章是否可访问
2. 判断尾部隐藏内容是否可访问
3. 校验访问验证码
4. 签发授权记录
5. 查询当前授权是否仍有效
6. 计算多规则组合是否全部满足

## 7.2 判定输入

判定时统一使用：

1. `resourceScope`
2. `resourceId`
3. `ruleTypes`
4. 当前登录用户
5. 当前访客标识 `visitorId`

## 7.3 登录规则判定

`LOGIN` 判定逻辑：

- 当前请求存在有效登录用户则通过
- 不写入长期授权表
- 不做跨登出缓存复用

## 7.4 文章访问码规则判定

`ACCESS_CODE` 判定逻辑：

1. 先检查当前 `resourceScope` 是否在该规则的 `supportedScopes` 内
2. 检查当前用户或访客是否已有未过期授权
3. 有则直接通过
4. 没有则返回未授权状态
5. 当前台提交 `accessCode` 后，服务端校验当前文章配置的访问码
6. 校验成功后写入 `content_access_grant`
7. `expires_at = now + 7d`

## 7.5 访问验证码规则判定

`WECHAT_ACCESS_CODE` 判定逻辑：

1. 检查当前用户或访客是否已有未过期授权
2. 有则直接通过
3. 没有则返回未授权状态
4. 当前台提交 `accessCode` 后，服务端校验
5. 校验成功后写入 `content_access_grant`
6. `expires_at = now + 7d`

## 7.6 多规则组合判定

对文章级权限，服务端不再只判断单个 `ruleType`，而是判断规则数组：

```json
["LOGIN", "ACCESS_CODE"]
```

判定原则：

1. 逐条计算每个规则是否已满足
2. 只有全部满足，文章才可访问
3. 若部分满足，则返回“未满足规则列表”

返回示例：

```json
{
  "granted": false,
  "pendingRules": ["ACCESS_CODE"]
}
```

这样前端可以只展示剩余未完成的步骤。

## 7.7 授权复用原则

为了避免文章级和尾部隐藏内容级重复要求输入验证码，建议增加“规则授权复用”策略：

- 同一篇文章下
- 同一规则类型
- 当前用户 / 访客已有有效授权

则允许尾部隐藏内容直接复用文章级授权，或文章复用上层授权策略。

正式建议：

- 先做“同文章、同规则复用”
- 后续再扩展更细粒度授权继承

补充约束：

- 当前产品版本下，尾部隐藏内容配置 UI 不开放 `ACCESS_CODE`
- 正文内嵌隐藏块留作后续 TODO

## 八、访客标识设计

为了支持未登录访客的验证码授权缓存，需要一个相对稳定的匿名访客标识。

正式结论：

- 不使用前端每次随机生成的 UUID
- 当前实现由服务端生成匿名访客指纹
- 当前实现以 `客户端 IP + User-Agent` 计算 MD5 后作为 `visitorId`

原因：

- 如果前端每次随机生成新的 UUID，则无法实现“7 天免重复输入”
- 服务端统一生成更容易保证详情接口和验证接口口径一致
- 单独只用纯 IP 容易在同一出口 IP 下发生串号，因此当前实现额外拼接 `User-Agent`

推荐做法：

- 详情接口和校验接口进入 Controller 时统一调用 `resolveVisitorId(request)`
- 优先从 `X-Forwarded-For` / `X-Real-IP` / `remoteAddr` 解析客户端 IP
- 再拼接 `User-Agent`
- 对 `IP + "|" + User-Agent` 做 MD5
- 结果仅在服务端内存与业务链路中使用，不对前端暴露

可信来源要求：

- 服务端不信任请求体中的 `visitorId`
- 当前实现也不再依赖前端传 `X-Yunyu-Visitor-Id`
- 服务端落库时只保存其哈希值：`visitor_id_hash`

补充建议：

- 如果当前用户已经登录，则优先使用 `userId` 作为授权主体
- 如果未登录，则使用 `visitorId` 作为授权缓存主体
- 如果后续出现匿名访客串号问题，再升级为“服务端签发第一方 visitor cookie + 指纹兜底”的二阶段方案

## 九、服务端内容返回策略

图源文件：

- [02-文章访问与尾部隐藏内容判定流程图.drawio](/Users/wangpenglong/projects/full-stack-project/Yunyu/docs/技术/资源/04-内容访问控制技术实现方案/drawio/02-文章访问与尾部隐藏内容判定流程图.drawio)
- [02-文章访问与尾部隐藏内容判定流程图.md](/Users/wangpenglong/projects/full-stack-project/Yunyu/docs/技术/资源/04-内容访问控制技术实现方案/mermaid/02-文章访问与尾部隐藏内容判定流程图.md)

这张图用于说明请求进入详情页后的实际判定顺序：先判断文章级访问权限，再判断尾部隐藏内容权限，最终由服务端决定返回正文、占位信息或真实尾部内容。

## 9.4 当前实现时序图

图源文件：

- [03-当前内容权限校验时序图.drawio](/Users/wangpenglong/projects/full-stack-project/Yunyu/docs/技术/资源/04-内容访问控制技术实现方案/drawio/03-当前内容权限校验时序图.drawio)
- [03-当前内容权限校验时序图.md](/Users/wangpenglong/projects/full-stack-project/Yunyu/docs/技术/资源/04-内容访问控制技术实现方案/mermaid/03-当前内容权限校验时序图.md)

这张图聚焦“当前代码已经落地的真实链路”，重点说明：

- 详情页首次进入调用哪个接口
- 服务端如何解析可选登录态与匿名访客指纹
- 文章未解锁时前端为什么只提示当前最高优先级规则
- 验证接口写入 `content_access_grant` 后，为什么还要再次请求详情接口

```mermaid
sequenceDiagram
    autonumber
    participant B as 浏览器 / 详情页
    participant A as 站点接口<br/>/api/site/posts/{slug}
    participant S as SiteContentService
    participant G as ContentAccessGrantService
    participant D as post / post_content / content_access_grant

    B->>A: GET /api/site/posts/{slug}
    A->>A: 解析可选登录态 + 生成访客指纹
    A->>S: getPostDetail(slug, visitorId)
    S->>D: 查询 post / post_content
    S->>G: hasValidGrant(scope, rule, userId, visitorIdHash)
    G->>D: 查询 content_access_grant
    G-->>S: 返回授权命中结果
    S-->>A: 返回 contentHtml / tailHiddenContentHtml / contentAccessState
    A-->>B: 返回正文内容与 pendingRules

    alt 文章级未通过
        B->>B: 只显示当前最高优先级规则
        alt 当前规则是 LOGIN
            B->>B: 跳转 /login?redirect=当前详情页
            B->>A: 登录后再次 GET /api/site/posts/{slug}
        else 当前规则是 ACCESS_CODE 或 WECHAT_ACCESS_CODE
            B->>A: POST /api/site/posts/{slug}/access/verify
            A->>A: 再次解析可选登录态 + 生成访客指纹
            A->>S: verifyContentAccess(slug, request, visitorId)
            S->>D: 查询 post / post_content
            S->>S: 校验规则是否启用 + 校验验证码
            S->>G: grantAccess(scope, postId, ruleType, userId, visitorIdHash)
            G->>D: 写入或刷新 content_access_grant（7天）
            G-->>S: 授权完成
            S-->>A: 返回最新 contentAccessState
            A-->>B: granted = true
            B->>A: 再次 GET /api/site/posts/{slug}
        end
    else 文章级已通过
        B->>B: 展示正文与评论区
        alt 尾部隐藏内容未通过
            B->>B: 仅显示尾部隐藏内容占位
            B->>A: 按需 POST /api/site/posts/{slug}/access/verify
            B->>A: 校验成功后再次 GET /api/site/posts/{slug}
        else 尾部隐藏内容也通过
            B->>B: 展示真实尾部隐藏内容
        end
    end
```

## 9.1 文章级策略

如果文章本身受限且当前未授权：

- 不返回真实正文
- 返回文章级访问状态
- 返回弹窗或拦截层所需元信息

如果已授权：

- 返回正常正文

## 9.2 尾部隐藏内容级策略

如果文章可访问，但尾部隐藏内容未授权：

- 正文主体正常返回
- 返回尾部隐藏内容状态
- 前台在正文结束后渲染占位卡片

如果已授权：

- 返回尾部隐藏内容 HTML
- 前台在正文结束后渲染真实尾部内容

## 9.3 为什么不建议前端自己拼

如果前端自己拼装正文中的受限内容：

- SSR 会更复杂
- 目录和锚点一致性更难保证
- 同一篇文章首屏 HTML 可能服务端和客户端不一致

因此建议：

- 服务端直接输出“当前请求视角下的最终 `contentHtml`”

## 十、接口设计

## 10.1 文章详情接口

现有接口：

- `GET /api/site/posts/{slug}`

建议扩展响应：

```json
{
  "id": 1,
  "title": "示例文章",
  "articleAccess": {
    "scope": "ARTICLE",
    "enabled": true,
    "rules": ["LOGIN", "ACCESS_CODE"],
    "granted": false,
    "pendingRules": ["ACCESS_CODE"],
    "actionType": "VERIFY_CODE",
    "message": "请完成剩余访问条件后查看全文"
  },
  "contentHtml": "",
  "tailHiddenAccess": {
    "scope": "ARTICLE_TAIL_HIDDEN",
    "enabled": true,
    "rules": ["WECHAT_ACCESS_CODE"],
    "granted": false,
    "pendingRules": ["WECHAT_ACCESS_CODE"],
    "actionType": "VERIFY_CODE",
    "message": "输入公众号验证码后可查看尾部隐藏内容"
  },
  "tailHiddenTitle": "尾部补充内容",
  "tailHiddenHtml": ""
}
```

说明：

- 若文章未授权，`contentHtml` 可以为空或仅包含拦截态安全占位内容
- 若文章已授权，则返回真实正文和尾部隐藏内容状态

## 10.2 访问验证码验证接口

当前实现接口：

- `POST /api/site/posts/{slug}/access/verify`

请求体示例：

```json
{
  "scopeType": "ARTICLE",
  "ruleType": "ACCESS_CODE",
  "accessCode": "ABC123"
}
```

说明：

- `accessCode` 是正式字段名
- 不再使用 `inviteCode`
- `visitorId` 不通过请求体传递，由服务端在 Controller 中统一生成
- `scopeType` 当前只允许：
  - `ARTICLE`
  - `TAIL_HIDDEN`

响应体示例：

```json
{
  "granted": true,
  "contentAccessState": {
    "articleAccessAllowed": true,
    "tailHiddenAccessAllowed": false,
    "articleAccessPendingRuleTypes": [],
    "tailHiddenAccessPendingRuleTypes": ["WECHAT_ACCESS_CODE"]
  }
}
```

## 十一、前端实现方案

## 11.1 统一弹窗组件

建议前端新增统一组件：

- `ContentAccessDialog`

支持两种模式：

- `LOGIN`
- `ACCESS_CODE`
- `WECHAT_ACCESS_CODE`

其中 `ACCESS_CODE` 模式展示：

- 文章访问码输入框
- 提交按钮
- 错误提示

其中 `WECHAT_ACCESS_CODE` 模式展示：

- 二维码
- 提示文案
- 验证码输入框
- 提交按钮
- 错误提示

## 11.2 统一类型定义

建议前端新增统一类型：

```ts
export type ContentAccessScope = 'ARTICLE' | 'ARTICLE_TAIL_HIDDEN'

export type ContentAccessRule = 'LOGIN' | 'ACCESS_CODE' | 'WECHAT_ACCESS_CODE'

export interface ContentAccessState {
  scope: ContentAccessScope
  resourceId: string
  rules: ContentAccessRule[]
  granted: boolean
  expiresAt: string | null
  pendingRules?: ContentAccessRule[]
  actionType: 'LOGIN' | 'ACCESS_CODE' | 'VERIFY_CODE' | 'NONE'
  message: string
}
```

## 11.3 前端交互流程

### 11.3.1 文章级

- 未授权时弹出访问弹窗
- 登录成功或验证码验证成功后，重新请求文章详情
- 如果是组合规则，则逐步消除 `pendingRules`

### 11.3.2 尾部隐藏内容级

- 点击尾部隐藏内容区域中的按钮，弹出同一套访问弹窗
- 验证成功后重新请求文章详情

## 11.4 `useApiClient` 扩展

建议统一附带：

- `Authorization`
- 匿名访客标识不再由前端生成，改为服务端在 Controller 中统一解析

## 十二、后台实现方案

## 12.1 文章级配置

后台文章编辑页需要支持：

- 公开 / 受限切换
- 规则多选

展示建议：

- 使用多选框或标签选择器
- 明确标注“需全部满足”
- 当选择 `ACCESS_CODE` 时，联动显示文章访问码配置项

## 12.2 尾部隐藏内容级配置

后台文章编辑页增加“尾部隐藏内容”配置区。

配置项建议：

- 是否启用尾部隐藏内容
- 尾部标题
- 规则类型
- Markdown 内容

限制：

- 当前产品版本不提供 `ACCESS_CODE`
- 后续如需开放，可通过规则元信息中的 `supportedScopes` 与配置层开关启用

## 十三、推荐开发顺序

1. 先抽象统一数据模型、规则数组与命名。
2. 再落文章级访问控制与 `ACCESS_CODE`。
3. 再接文章级多规则组合判定。
4. 再接尾部隐藏内容级访问控制。
5. 再实现统一弹窗和统一验证接口。
6. 最后做授权复用与缓存优化。

## 十四、扩展性结论

这套方案的价值不只是解决当前“文章权限 + 尾部隐藏内容”。

它后续还能自然扩展到：

- 会员内容
- 专题访问码
- 商品购买后可见内容
- 下载资源授权

因此本期最重要的不是做一个临时功能，而是把“内容访问控制”作为正式基础设施搭好。

## 十五、后续 TODO

正文内嵌隐藏块保留为后续思考方向，但当前版本不做。

后续如果需要，可再扩展：

- Markdown 正文中插入多个隐藏块
- 不同块使用不同规则
- 正文中间位置展示受限内容

## 十六、数据库字段清单

本节用于把当前方案进一步收口到可执行的数据层。

第一期建议优先采用“正文内容字段单独存储 + 访问控制配置收敛到 JSON + 新增统一授权表”的方式，不额外引入过重的资源配置中心。

## 16.1 `post_content` 表新增字段建议

用于承载正文内容、尾部隐藏内容正文和统一访问控制配置。

| 字段名 | 类型建议 | 是否必填 | 默认值 | 说明 |
|---|---|---|---|---|
| `content_access_config_json` | `json` | 否 | `null` | 统一内容访问控制配置 |
| `tail_hidden_content_markdown` | `mediumtext` | 否 | `null` | 尾部隐藏内容 Markdown |
| `tail_hidden_content_html` | `mediumtext` | 否 | `null` | 尾部隐藏内容 HTML |

补充建议：

- `content_access_config_json` 建议包含 `version` 字段，便于后续升级结构。
- `content_access_config_json` 中的 `articleAccess` 与 `tailHiddenAccess` 建议按对象结构管理，而不是扁平 key。
- `tail_hidden_content_html` 的生成策略应与正文 `contentHtml` 保持一致。

建议 JSON 结构示例：

```json
{
  "version": 1,
  "articleAccess": {
    "enabled": true,
    "ruleTypes": ["LOGIN", "ACCESS_CODE"],
    "articleAccessCode": "ABC123",
    "articleAccessCodeHint": "请输入文章访问码"
  },
  "tailHiddenAccess": {
    "enabled": true,
    "title": "尾部补充内容",
    "ruleTypes": ["WECHAT_ACCESS_CODE"]
  }
}
```

## 16.2 `site_config` 表新增字段建议

用于承载站点级公众号验证码能力。

| 字段名 | 类型建议 | 是否必填 | 默认值 | 说明 |
|---|---|---|---|---|
| `wechat_access_code` | `varchar(128)` | 否 | `null` | 公众号验证码 |
| `wechat_access_code_hint` | `varchar(255)` | 否 | `null` | 公众号验证码提示文案 |
| `wechat_qr_code_url` | `varchar(512)` | 否 | `null` | 公众号二维码图片地址 |
| `wechat_access_code_expires_in_days` | `int` | 是 | `7` | 授权免重复输入天数 |

## 16.3 `content_access_grant` 表字段清单

用于承载用户 / 访客维度的授权缓存。

| 字段名 | 类型建议 | 是否必填 | 说明 |
|---|---|---|---|
| `id` | `bigint` | 是 | 主键 |
| `resource_scope` | `varchar(64)` | 是 | `ARTICLE` / `ARTICLE_TAIL_HIDDEN` |
| `resource_id` | `varchar(128)` | 是 | 资源标识 |
| `parent_article_id` | `bigint` | 否 | 统一归属文章 ID |
| `rule_type` | `varchar(64)` | 是 | `LOGIN` / `ACCESS_CODE` / `WECHAT_ACCESS_CODE` |
| `grant_subject_type` | `varchar(32)` | 是 | `USER` / `VISITOR` |
| `user_id` | `bigint` | 否 | 登录用户 ID |
| `visitor_id_hash` | `varchar(128)` | 否 | 访客标识哈希值 |
| `grant_key` | `varchar(255)` | 是 | 去重与快速命中用业务键 |
| `granted_at` | `datetime` | 是 | 授权时间 |
| `expires_at` | `datetime` | 是 | 过期时间 |
| `status` | `varchar(32)` | 是 | `ACTIVE` / `EXPIRED` / `REVOKED` |
| `created_at` | `datetime` | 是 | 创建时间 |
| `updated_at` | `datetime` | 是 | 更新时间 |

索引建议：

- 唯一索引：`uk_grant_key(grant_key)`
- 普通索引：`idx_resource_scope_resource_id(resource_scope, resource_id)`
- 普通索引：`idx_user_id(user_id)`
- 普通索引：`idx_visitor_id_hash(visitor_id_hash)`
- 普通索引：`idx_expires_at(expires_at)`

## 十七、后端 DTO / VO 草案

本节用于约束第一期后端接口契约与命名风格，便于后续直接落 Controller / Service / VO。

## 17.1 管理端 DTO 草案

### 17.1.1 `AdminPostContentAccessConfigRequest`

用于统一承载 `contentAccessConfig`。

```java
public class AdminPostContentAccessConfigRequest {
    private Integer version;
    private ArticleAccessConfigRequest articleAccess;
    private TailHiddenAccessConfigRequest tailHiddenAccess;

    public static class ArticleAccessConfigRequest {
        private Boolean enabled;
        private List<String> ruleTypes;
        private String articleAccessCode;
        private String articleAccessCodeHint;
    }

    public static class TailHiddenAccessConfigRequest {
        private Boolean enabled;
        private String title;
        private List<String> ruleTypes;
    }
}
```

### 17.1.2 合入现有文章创建 / 更新请求

如果当前项目文章创建与更新已经存在：

- `AdminPostCreateRequest`
- `AdminPostUpdateRequest`

建议将下列字段直接合入现有请求对象，而不是再单独开一组文章访问配置接口：

- `contentAccessConfig`
- `tailHiddenContentMarkdown`

## 17.2 站点配置 DTO 草案

### 17.2.1 `AdminSiteContentAccessConfigRequest`

用于站点级公众号验证码配置。

```java
public class AdminSiteContentAccessConfigRequest {
    private String wechatAccessCode;
    private String wechatAccessCodeHint;
    private String wechatQrCodeUrl;
    private Integer wechatAccessCodeExpiresInDays;
}
```

如果当前项目已有：

- `AdminSiteConfigUpdateRequest`

建议直接把以上字段并入现有站点配置更新请求。

## 17.3 站点端请求 DTO 草案

### 17.3.1 `SiteContentAccessVerifyCodeRequest`

用于统一验证码验证接口。

```java
public class SiteContentAccessVerifyCodeRequest {
    private String resourceScope;
    private String resourceId;
    private String ruleType;
    private String accessCode;
}
```

说明：

- 不在请求体中暴露 `visitorId`
- `visitorId` 由服务端从 Cookie 或 Header 中读取

## 17.4 站点端 VO 草案

### 17.4.1 `SiteContentAccessStateResponse`

```java
public class SiteContentAccessStateResponse {
    private String scope;
    private String resourceId;
    private Boolean enabled;
    private List<String> rules;
    private Boolean granted;
    private String actionType;
    private String message;
    private String expiresAt;
    private List<String> pendingRules;
}
```

### 17.4.2 `SiteContentAccessVerifyCodeResponse`

```java
public class SiteContentAccessVerifyCodeResponse {
    private Boolean granted;
    private String expiresAt;
    private String message;
}
```

### 17.4.3 `SitePostDetailResponse` 扩展字段草案

建议在现有 `SitePostDetailResponse` 中新增：

```java
private SiteContentAccessStateResponse articleAccess;
private SiteContentAccessStateResponse tailHiddenAccess;
private String tailHiddenTitle;
private String tailHiddenHtml;
```

补充说明：

- 若文章未授权，`contentHtml` 返回空字符串或安全占位内容
- 若尾部隐藏内容未授权，`tailHiddenHtml` 返回空字符串

## 十八、前端类型草案

本节用于约束 `yunyu-web/app/types` 与 composable 调用契约。

## 18.1 统一内容访问类型

建议新增文件：

- `app/types/content-access.ts`

类型草案：

```ts
export type ContentAccessScope = 'ARTICLE' | 'ARTICLE_TAIL_HIDDEN'

export type ContentAccessRule = 'LOGIN' | 'ACCESS_CODE' | 'WECHAT_ACCESS_CODE'

export type ContentAccessActionType = 'LOGIN' | 'ACCESS_CODE' | 'VERIFY_CODE' | 'NONE'

export interface ContentAccessState {
  scope: ContentAccessScope
  resourceId: string
  enabled: boolean
  rules: ContentAccessRule[]
  granted: boolean
  expiresAt: string | null
  pendingRules: ContentAccessRule[]
  actionType: ContentAccessActionType
  message: string
}

export interface SiteContentAccessVerifyCodeRequest {
  resourceScope: ContentAccessScope
  resourceId: string
  ruleType: ContentAccessRule
  accessCode: string
}

export interface SiteContentAccessVerifyCodeResponse {
  granted: boolean
  expiresAt: string | null
  message: string
}
```

## 18.2 `SitePostDetail` 扩展字段草案

建议在现有 `app/types/site.ts` 中的 `SitePostDetail` 增加：

```ts
  articleAccess: ContentAccessState | null
  tailHiddenAccess: ContentAccessState | null
  tailHiddenTitle: string
  tailHiddenHtml: string
```

## 18.3 管理端文章编辑类型草案

建议补充管理端文章编辑表单类型：

```ts
export interface AdminPostAccessConfig {
  articleAccess: {
    enabled: boolean
    ruleTypes: ContentAccessRule[]
    articleAccessCode: string
    articleAccessCodeHint: string
  }
  tailHiddenAccess: {
    enabled: boolean
    title: string
    ruleTypes: ContentAccessRule[]
  }
  tailHiddenContentMarkdown: string
}
```

补充约束：

- `tailHiddenAccess.ruleTypes` 第一期 UI 层仅允许选择 `LOGIN` 或 `WECHAT_ACCESS_CODE`
- `articleAccess.articleAccessCode` 仅在 `articleAccess.ruleTypes` 包含 `ACCESS_CODE` 时展示和提交

## 18.4 前端 Header / Cookie 协议草案

建议统一约定：

- 登录用户仍通过 `Authorization` 识别
- 未登录访客当前由服务端根据 `IP + User-Agent` 生成匿名访客指纹
- 如果后续需要进一步降低串号风险，再升级为“服务端签发第一方 visitor cookie”

前端侧要求：

- 所有详情请求与验证码验证请求统一走现有站点接口
- 不通过请求体传递 `visitorId`
- 登录态仍只通过 `Authorization` 透传

## 十九、SQL DDL 草案

本节给出第一期建议 SQL 草案，便于后续迁移脚本直接落地。

说明：

- 以下 SQL 以 MySQL 8.x 风格示意
- 若当前项目已有统一建表字段规范，应按项目现有规范微调
- `json` 字段若当前环境不便使用，可先降级为 `varchar`

## 19.1 `post` 表加字段草案

```sql
ALTER TABLE post_content
    ADD COLUMN content_access_config_json JSON NULL COMMENT '统一内容访问控制配置',
    ADD COLUMN tail_hidden_content_markdown MEDIUMTEXT NULL COMMENT '尾部隐藏内容 Markdown',
    ADD COLUMN tail_hidden_content_html MEDIUMTEXT NULL COMMENT '尾部隐藏内容 HTML';
```

## 19.2 `post_content` 表 JSON 结构草案

```json
{
  "version": 1,
  "articleAccess": {
    "enabled": true,
    "ruleTypes": ["LOGIN", "ACCESS_CODE"],
    "articleAccessCode": "ABC123",
    "articleAccessCodeHint": "请输入文章访问码"
  },
  "tailHiddenAccess": {
    "enabled": true,
    "title": "尾部补充内容",
    "ruleTypes": ["WECHAT_ACCESS_CODE"]
  }
}
```

说明：

- 如果当前数据库环境不方便使用 `JSON` 字段，第一期也可以临时退化为 `LONGTEXT`
- 但服务端对象模型仍建议按结构化 JSON 读写

## 19.3 `site_config` 表加字段草案

```sql
ALTER TABLE site_config
    ADD COLUMN wechat_access_code VARCHAR(128) NULL COMMENT '站点级公众号验证码',
    ADD COLUMN wechat_access_code_hint VARCHAR(255) NULL COMMENT '公众号验证码提示文案',
    ADD COLUMN wechat_qr_code_url VARCHAR(512) NULL COMMENT '公众号二维码地址',
    ADD COLUMN wechat_access_code_expires_in_days INT NOT NULL DEFAULT 7 COMMENT '公众号验证码授权有效天数';
```

## 19.4 `content_access_grant` 表建表草案

```sql
CREATE TABLE content_access_grant (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
    resource_scope VARCHAR(64) NOT NULL COMMENT '资源范围',
    resource_id VARCHAR(128) NOT NULL COMMENT '资源标识',
    parent_article_id BIGINT NULL COMMENT '归属文章ID',
    rule_type VARCHAR(64) NOT NULL COMMENT '规则类型',
    grant_subject_type VARCHAR(32) NOT NULL COMMENT '授权主体类型',
    user_id BIGINT NULL COMMENT '登录用户ID',
    visitor_id_hash VARCHAR(128) NULL COMMENT '访客标识哈希值',
    grant_key VARCHAR(255) NOT NULL COMMENT '授权唯一业务键',
    granted_at DATETIME NOT NULL COMMENT '授权时间',
    expires_at DATETIME NOT NULL COMMENT '过期时间',
    status VARCHAR(32) NOT NULL DEFAULT 'ACTIVE' COMMENT '状态',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_grant_key (grant_key),
    KEY idx_resource_scope_resource_id (resource_scope, resource_id),
    KEY idx_parent_article_id (parent_article_id),
    KEY idx_user_id (user_id),
    KEY idx_visitor_id_hash (visitor_id_hash),
    KEY idx_expires_at (expires_at)
) COMMENT='内容访问授权缓存表';
```

## 二十、Java 类文件清单

本节按照当前仓库真实结构，列出第一期建议新增或修改的 Java 文件。

## 20.1 文章模块建议修改文件

已有文件，建议修改：

- [AdminPostCreateRequest.java](/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-server/src/main/java/com/ideaflow/yunyu/module/post/dto/AdminPostCreateRequest.java)
- [AdminPostUpdateRequest.java](/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-server/src/main/java/com/ideaflow/yunyu/module/post/dto/AdminPostUpdateRequest.java)
- [PostEntity.java](/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-server/src/main/java/com/ideaflow/yunyu/module/post/entity/PostEntity.java)
- [PostContentEntity.java](/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-server/src/main/java/com/ideaflow/yunyu/module/post/entity/PostContentEntity.java)
- [PostMapper.java](/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-server/src/main/java/com/ideaflow/yunyu/module/post/mapper/PostMapper.java)
- [PostContentMapper.java](/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-server/src/main/java/com/ideaflow/yunyu/module/post/mapper/PostContentMapper.java)
- [AdminPostService.java](/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-server/src/main/java/com/ideaflow/yunyu/module/post/service/AdminPostService.java)
- [AdminPostItemResponse.java](/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-server/src/main/java/com/ideaflow/yunyu/module/post/vo/AdminPostItemResponse.java)
- [AdminPostController.java](/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-server/src/main/java/com/ideaflow/yunyu/module/post/controller/AdminPostController.java)

改动职责：

- 文章创建 / 更新请求接入访问控制配置字段
- 文章实体与内容实体补新字段
- 管理端详情回填访问控制配置

## 20.2 站点配置模块建议修改文件

已有文件，建议修改：

- [AdminSiteConfigUpdateRequest.java](/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-server/src/main/java/com/ideaflow/yunyu/module/site/dto/AdminSiteConfigUpdateRequest.java)
- [SiteConfigEntity.java](/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-server/src/main/java/com/ideaflow/yunyu/module/site/entity/SiteConfigEntity.java)
- [SiteConfigMapper.java](/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-server/src/main/java/com/ideaflow/yunyu/module/site/mapper/SiteConfigMapper.java)
- [AdminSiteConfigService.java](/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-server/src/main/java/com/ideaflow/yunyu/module/site/service/AdminSiteConfigService.java)
- [AdminSiteConfigResponse.java](/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-server/src/main/java/com/ideaflow/yunyu/module/site/vo/AdminSiteConfigResponse.java)
- [AdminSiteConfigController.java](/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-server/src/main/java/com/ideaflow/yunyu/module/site/controller/AdminSiteConfigController.java)

改动职责：

- 接入 `WECHAT_ACCESS_CODE` 的站点级配置
- 后台站点设置页支持保存与回显

## 20.3 站点内容模块建议修改文件

已有文件，建议修改：

- [SiteContentController.java](/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-server/src/main/java/com/ideaflow/yunyu/module/site/controller/SiteContentController.java)
- [SiteContentService.java](/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-server/src/main/java/com/ideaflow/yunyu/module/site/service/SiteContentService.java)
- [SitePostDetailResponse.java](/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-server/src/main/java/com/ideaflow/yunyu/module/site/vo/SitePostDetailResponse.java)

改动职责：

- 文章详情接口增加 `articleAccess / tailHiddenAccess / tailHiddenTitle / tailHiddenHtml`
- 文章详情服务增加访问控制判定与裁剪返回逻辑

## 20.4 建议新增内容访问控制模块

建议新增目录：

- `module/contentaccess`

建议新增文件：

- `controller/SiteContentAccessController.java`
- `service/ContentAccessService.java`
- `mapper/ContentAccessGrantMapper.java`
- `entity/ContentAccessGrantEntity.java`
- `dto/SiteContentAccessVerifyCodeRequest.java`
- `vo/SiteContentAccessStateResponse.java`
- `vo/SiteContentAccessVerifyCodeResponse.java`

建议职责：

- 统一验证码验证接口
- 统一授权缓存读写
- 统一多规则组合判定

## 20.5 可选新增 Convert 文件

如果当前项目希望保持对象转换集中，建议新增：

- `module/contentaccess/convert/ContentAccessConvert.java`

职责：

- 统一完成 Entity / DTO / VO 转换

## 二十一、前端改动文件清单

本节按照当前 `yunyu-web` 真实结构，列出第一期建议新增或修改文件。

## 21.1 建议新增文件

- `/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-web/app/types/content-access.ts`
- `/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-web/app/components/content-access/ContentAccessDialog.vue`
- `/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-web/app/composables/useContentAccess.ts`

职责：

- 统一放内容访问控制类型
- 统一弹窗组件
- 统一验证码验证请求封装

## 21.2 建议修改文件

### 21.2.1 站点详情页链路

- [site.ts](/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-web/app/types/site.ts)
- [useSiteContent.ts](/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-web/app/composables/useSiteContent.ts)
- [[slug].vue](/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-web/app/pages/posts/[slug].vue)
- [useApiClient.ts](/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-web/app/composables/useApiClient.ts)

改动职责：

- `SitePostDetail` 增加访问控制相关字段
- 文章详情页接入文章级拦截与尾部隐藏内容占位
- 请求链路统一携带登录态；匿名访客指纹由服务端生成

### 21.2.2 管理端文章编辑链路

建议优先排查并修改现有文章编辑相关文件，例如：

- [AdminPostEditorScreen.vue](/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-web/app/components/admin/post/AdminPostEditorScreen.vue)

如当前项目还存在对应的文章编辑 composable / 表单类型，也应同步修改。

改动职责：

- 新增文章访问控制配置区
- 新增尾部隐藏内容配置区
- 接入文章访问码输入项
- 限制尾部隐藏内容第一期不开放 `ACCESS_CODE`

### 21.2.3 管理端站点设置链路

- [site.vue](/Users/wangpenglong/projects/full-stack-project/Yunyu/yunyu-web/app/pages/admin/site.vue)

改动职责：

- 新增公众号验证码配置项
- 新增公众号二维码配置项
- 新增授权有效天数配置项

## 21.3 建议开发拆分顺序

前端建议按下面顺序拆任务：

1. 先补 `app/types/content-access.ts`
2. 再改 `app/types/site.ts`
3. 再补 `useContentAccess.ts`
4. 再扩展 `useApiClient.ts`
5. 再做 `ContentAccessDialog.vue`
6. 再接入文章详情页
7. 最后接入管理端文章编辑页与站点设置页
